# ThreatScope Observatory — Python dependencies
# Install with:  pip install -r requirements.txt

# Core data
requests>=2.31.0
pandas>=2.0.0

# Visualisation
matplotlib>=3.7.0
plotly>=5.18.0

# Dashboard
# streamlit>=1.40.0 required to avoid ModuleNotFoundError: altair.vegalite.v4
streamlit>=1.40.0

# altair>=5.0.0 required — Streamlit 1.40+ uses Altair 5 internally;
# older versions shipped altair.vegalite.v4 which was removed in Altair 5.
altair>=5.0.0
